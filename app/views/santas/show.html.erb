<div class="scroll-container">
  <div class="container">
    <p
      id="welcome"
      class="p-cloud"
      style="font-size: 1.5rem; opacity: 1; transition: opacity 1s"
    >
      Welcome, <%= @santa.name %>.
    </p>

    <p class="p-cloud" style="font-size: 1.4rem; margin-bottom: 0;">
      Please get a gift for
    </p>
    <p
      id="giftee"
      class="p-cloud"
      style="filter: blur(8px); font-size: 1.4rem; margin-top: 0;"
    >
      someone special
    </p>

    <p class="p-cloud"id="giftee-preferences">
      <% if @santa.recipient.preferences.length > 0 %>
        They have shared some gift preferences:
        <pre><%= @santa.recipient.preferences %></pre>
      <% else %>
        <%= @santa.recipient.pronoun %> has not yet shared any gift preferences. Check back later after everyone has had a chance to fill those in!
      <% end %>
    </p>

    <p
      id="reveal"
      class="p-cloud"
      style="display: flex; align-items: center; font-size: 20px; margin: 0;"
    >
      Click to reveal.
      <button
        style="transform: scaleY(0.7); border: none; background: none; cursor: pointer"
        onClick="revealGiftee()"
      >
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="40">
          <circle cx="32" cy="32" r="10" fill="#444"/>
          <circle cx="32" cy="32" r="6" fill="#fff"/>
          <path
            d="M32 4C15 4 4 32 4 32s11 28 28 28 28-28 28-28S49 4 32 4zm0 48c-14.36 0-24-20-24-20s9.64-20 24-20 24 20 24 20-9.64 20-24 20z"
            fill="#444"
          />
        </svg>
      </button>
    </p>

    <div
      id="gift-value"
      style="
        display: flex; flex-direction: column; align-items: center;
        font-size: 20px; opacity: 0; transition: opacity 1s; margin: 0
      "
    >
      <p
        class="p-cloud"
      >
        Let's keep the gifts no more than $200 in value.
      </p>
    </div>

    <p class="p-cloud">Please take a moment to share your gift preferences to help Santa find the perfect gift for you...</p>

    <%= form_with model: @santa, url: "/#{@santa.permalink}", method: :post do |form| %>
      <%= form.text_area :preferences, class: "preferences-textarea" %>
      <%= form.submit "Tell Santa what you want!", class: "submit-button" %>
    <% end %>
  </div>
</div>

<script>
  window.revealGiftee = () =>{
    const span = document.getElementById('giftee');
    span.style.filter = 'none';
    span.textContent = '<%= @santa.recipient.name %>!';
    document.getElementById('reveal').style.opacity = '0';
    document.getElementById('giftee-preferences').classList.add('visible');
    document.getElementById('gift-value').style.opacity = '1';
  }
</script>

<style>
  p {
    font-size: 1.2rem;
  }
  .p-cloud {
    --sx: 3.0;              /* >1 = wider ellipse, tune for your 8:1 look */
    --blur: 12px;

    position: relative;
    display: inline-block;
    padding: 1rem 2rem;
    z-index: 0;
  }

  .p-cloud::before {
    content: "";
    position: absolute;
    inset: -26px;
    z-index: -1;
    pointer-events: none;

    background: radial-gradient(
      closest-side,
      rgba(255,255,255,0.9) 0%,
      rgba(255,255,255,0.9) 50%,
      rgba(255,255,255,0.8) 90%,
      rgba(255,255,255,0.00) 100%
    );

    /* the trick: stretch the “cloud” horizontally */
    transform: scaleX(var(--sx));
    transform-origin: center;

    /* and compensate so it still *covers* the same area visually */
    /* (we counter-scale by making it narrower before scaling) */
    width: calc(100% / var(--sx));
    left: calc((100% - (100% / var(--sx))) / 2);

    filter: blur(var(--blur));
    border-radius: 999px;
  }

  .preferences-textarea {
    background-color: rgba(255, 255, 255, 0.85);
    width: 100%;
    border-radius: 16px;
    height: 10rem;
    margin-top: 20px;
    font-size: 16px;
    padding: 10px;
    border: 1px solid #ccc;
    box-sizing: border-box;
    font-family: 'Arial', sans-serif;
  }
  .submit-button {
    margin: 0px auto;
    padding: 1rem 2rem;
    background-color: #ff6F60;
    border: 3px solid #ff6F60;
    font-size: 1.3rem;
    color: white;
    border-radius: 16px;
    cursor: pointer;
  }

  #giftee-preferences {
    display: none;
  }
  #giftee-preferences.visible {
    display: block;
    margin-top: 20px;
    font-size: 1.2rem;
  }
  #giftee-preferences pre {
    background-color: #f4f4f4;
    padding: 10px;
    border-radius: 8px;
    white-space: pre-wrap;
    word-wrap: break-word;
  }
  form {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
  }
  .scroll-container {
    height: 100vh;
    overflow-y: auto;
    width: 100vw;
  }

  .container {
    width: 600px;
    max-width: 90%;
    height: 100%;
    margin: 0 auto;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    padding: 40px 0;
    font-family: 'Arial', sans-serif;
  }
</style>
